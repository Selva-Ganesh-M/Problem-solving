# def sorting(arr, i=0, j=1, clear=1):
#   if len(arr)==1: return;
#   if j==len(arr):
#     return sorting(arr) if not clear else None;
#   if (arr[i]>arr[j]):
#     arr[i], arr[j] = arr[j], arr[i];
#     clear = 0
#   return sorting(arr, i+1, j+1, clear)

# arr=[9,8,7,6,5,1,2,3,4,5]
# sorting(arr)
# print(arr)
